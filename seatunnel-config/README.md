# Seatunnel Config

The `seatunnel-config` module provides a unified configuration loading and parsing mechanism for SeaTunnel, built on top of [Typesafe Config](https://github.com/lightbend/config).

It centralizes configuration logic, supporting multiple formats (HOCON, JSON, Properties) and offering utilities for configuration shading and variable substitution.